# Task - Fixing Pause/Resume "Crash" and Improving Stability

- `[ ]` Update `DownloadResult.kt` with pause/stop flags
- `[ ]` Update `PythonBridge.kt` to parse these flags
- `[ ]` Update `DownloadEngine.kt` to handle `PAUSED` status and suppress errors
- `[ ]` Update `DownloadWorker.kt` to handle batch results gracefully
- `[ ]` Verify UI behavior (Pause/Resume cycle)
